ENV: "debug"
BASE_URL:
HOST: "127.0.0.1"
PORT: "8081"

JWT_SECRET: "123456"
JWT_ISSUER: "SEIGA"

POSTGRES_USERNAME: "postgres"
POSTGRES_PASSWORD: "postgres"
POSTGRES_HOST: "127.0.0.1"
POSTGRES_PORT: "5432"
POSTGRES_DATABASE: "test_rsud"

DB_POOL_MAX_IDLE_CON: 5
DB_POOL_MAX_OPEN_CON: 10
DB_POOL_CON_LIFETIME_IN_MINUTE: 1

SMTP_HOST:
SMTP_PORT:
SMTP_PASSWORD:
SMTP_USERNAME:
SMTP_SENDER:

MINIO_ENDPOINT:
MINIO_USERNAME:
MINIO_PASSWORD:
MINIO_SSL:
MINIO_BUCKET:
CDN_BASE_URL:
FORCE_LOCAL_STORAGE: true

REDIS_HOST: "127.0.0.1"
REDIS_PORT: "6379"
REDIS_USERNAME:
REDIS_PASSWORD:
REDIS_DIAL_TIMEOUT: 30000
REDIS_READ_TIMEOUT: 30000
REDIS_WRITE_TIMEOUT: 30000

LOG_USE_FILE: true

USE_OLTP_GRAFANA: false
OTEL_SERVICE_NAME: "my-template-service"
OTEL_INSTANCE_ID: "instance-1"

OTLP_TRACE_HTTP: "localhost:4318"
OTLP_TRACE_USE_HTTPS: false

OTLP_METRIC_HTTP: "localhost:4318"
OTLP_METRIC_USE_HTTPS: false
OTEL_METRIC_EXPORT_INTERVAL_IN_S: 5

OTLP_LOG_HTTP: "localhost:4318"
OTLP_LOG_USE_HTTPS: false
